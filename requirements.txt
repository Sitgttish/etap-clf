# ETAP-CLF — runtime dependencies
# Install:  pip install -r requirements.txt
# Notebooks were run on Google Colab (GPU); versions below are the floors the code assumes.

# Core numerics / data
numpy>=1.24
pandas>=2.0
scipy>=1.11
h5py>=3.9

# Protein language models
torch>=2.1
esm>=3.0                 # ESM3 (esm3_sm_open_v1); requires a HuggingFace token
transformers>=4.40       # ESM-2 (facebook/esm2_t33_650M_UR50D) for the PLM-FRP baseline
huggingface_hub>=0.23

# Modelling / evaluation
scikit-learn>=1.3
xgboost>=2.0             # FRP-XGBoost and PLM-FRP baselines

# Sequences
biopython>=1.81

# Figures
matplotlib>=3.7
seaborn>=0.13
umap-learn>=0.5          # Figure 2 UMAP projections
Pillow>=10.0

tqdm>=4.66

# External tools (not pip-installable)
#   MMseqs2 >= 13   — redundancy reduction at 0.30 sequence identity
